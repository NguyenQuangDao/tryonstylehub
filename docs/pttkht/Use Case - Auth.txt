flowchart LR

    subgraph System[Hệ thống TryOnStyleHub]
        subgraph Auth[Đăng nhập - chi tiết]
            Auth_EmailPwd([Email/Mật khẩu])
            Auth_Token([Làm mới token])
            Auth_Profile([Quản lý hồ sơ])
            Auth_EmailPwd --> Auth_Token
            Auth_Token --> Auth_Profile
        end
    end
